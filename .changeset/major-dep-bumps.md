---
'@benhigham/stylelint-config': minor
---

- Bump `stylelint-order` from v7 to v8 — adds CSS-in-JS interpolation autofix support; no config changes required
- Bump `stylelint-gamut` from v1 to v2 — migrated to ESM, adds Stylelint v17 support; no config changes required
